namespace ERROR_MESSAGES {
  export const enum FAILED {
    login = "Login failed",
    signup = "Signup failed",
    email = "Email failed",
    auth = "Auth Failed",
  }
}

export default ERROR_MESSAGES;
